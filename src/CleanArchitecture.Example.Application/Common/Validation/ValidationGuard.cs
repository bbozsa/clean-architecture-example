namespace CleanArchitecture.Example.Application.Common.Validation
{
    public static class ValidationGuard
    {
        //public static void ValidateRequest<TRequest>(IValidator validator, TRequest request)
        //{
        //    if (validator == null)
        //    {
        //        throw new ArgumentNullException(nameof(validator), "Validator is missing.");
        //    }

        //    var validationResult = validator.Validate(request);
        //    if (!validationResult.IsValid)
        //    {
        //        throw new ValidationException(validationResult.Errors);
        //    }
        //}
    }
}
